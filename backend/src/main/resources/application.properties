# Server configuration
server.port=8080

# Logging
logging.level.com.zeotap=DEBUG

# File upload settings
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# Temporary file location for uploads
app.upload.dir=${java.io.tmpdir}/data-ingestion/uploads/ 